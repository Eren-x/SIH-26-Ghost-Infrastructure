{
  "name": "ghost-rover",
  "version": "1.0.0",
  "description": "Real-time 3D digital twin simulation of an autonomous infrastructure inspection rover with multi-sensor anomaly detection",
  "private": true,
  "type": "module",
  "license": "MIT",
  "author": "Dipansh Das",
  "repository": {
    "type": "git",
    "url": "git@github.com:Eren-x/SIH-26-Ghost-Infrastructure.git"
  },
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "node server/server.js",
    "client": "npm run --prefix client dev",
    "build": "npm install --prefix server && npm install --prefix client && npm run build --prefix client",
    "start": "node server/server.js",
    "postinstall": "npm install --prefix server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
